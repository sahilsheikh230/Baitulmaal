<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Baitulmaal Admin Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fc;
      font-family: 'Segoe UI', sans-serif;
    }
    .sidebar {
      width: 250px;
      height: 100vh;
      background-color: #1b1f3b;
      color: white;
      position: fixed;
      padding-top: 20px;
    }
    .sidebar a {
      color: #cfd8dc;
      text-decoration: none;
      display: block;
      padding: 10px 20px;
    }
    .sidebar a:hover {
      background-color: #394263;
      color: white;
    }
    .main {
      margin-left: 250px;
      padding: 30px;
    }
    .card-box {
      border-radius: 10px;
      background-color: white;
      padding: 20px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.05);
      text-align: center;
    }
    .project-table th, .project-table td {
      vertical-align: middle;
    }
    .priority-badge {
      font-size: 0.85rem;
    }
    .btn-view {
      font-size: 0.85rem;
      padding: 6px 14px;
      background-color: #0d6efd;
      color: white;
      border-radius: 20px;
      text-decoration: none;
      transition: 0.3s ease;
    }
    .btn-view:hover {
      background-color: #0b5ed7;
      color: white;
    }
  </style>
</head>
<body>
  <div class="sidebar">
    <h4 class="text-center mb-4">Baitulmaal Admin</h4>
    <a href="/home/admindashboard"><i class="fas fa-chart-line me-2"></i> Dashboard</a>
    <a href="/home/admin/donations"><i class="fas fa-donate me-2"></i> Donations</a>
    <a href="/admin/viewreports"><i class="fas fa-file-alt me-2"></i> Reports</a>
    <a href="#"><i class="fas fa-cogs me-2"></i> Settings</a>
    
    <a href="/home"> <i class="fas fa-arrow-left me-2"></i>Back to Home Page</a>
  </div>

  <div class="main">
    <h2 class="mb-4">Welcome, Admin</h2>

    <div class="row g-4 mb-4">
      <div class="col-md-3">
        <div class="card-box">
          <h5>Registered Baitulmaals</h5>
          <h2><%=length%></h2>
        </div>
      </div>
      <div class="col-md-3">
        <div class="card-box">
          <h5>Verified Baitulmaals</h5>
          <h2><%=length-count%></h2>
        </div>
      </div>
      <div class="col-md-3">
        <div class="card-box">
          <h5>Pending Baitulmaals</h5>
          <h2><%=count-rejectedcount%></h2>
        </div>
      </div>
<div class="col-md-3">
        <div class="card-box">
          <h5>Rejected Baitumaals</h5>
          <h2><%=rejectedcount%></h2>
        </div>
      </div>


    </div>

    <div class="card shadow-sm">
      <div class="card-header bg-white fw-bold">All Baitulmaals</div>
      <div class="card-body">
        <table class="table table-hover project-table">
          <thead>
            <tr>
              <th><i class="fas fa-building me-1 text-secondary"></i>Organisation Name</th>
              <th><i class="fas fa-location-dot me-1 text-secondary"></i>Address</th>
              <th><i class="fas fa-phone me-1 text-secondary"></i>Phone</th>
              <th>Action</th>
            </tr>
          </thead>
          <tbody>
            <% for(baitulmaal of baitulmaals){ %>
              <tr>
                <td><i class="fas fa-building text-primary me-2"></i> <%= baitulmaal.name %>
                <%if(baitulmaal.isverified){%>
                  <span class="badge bg-success">
  <i class="fas fa-check-circle me-1"></i> Verified
</span>
               <% }%>
                 <%if(baitulmaal.status==='Rejected'){%>
<span class="badge bg-danger">
  <i class="fas fa-times-circle me-1"></i> Rejected
</span>


              <%   }%>
                
                </td>
                <td><span class="badge  text-dark priority-badge"><%= baitulmaal.address %></span></td>
                <td><i class="fas fa-phone-alt text-success me-2"></i> <%= baitulmaal.phone %></td>
                <td><a href="/admin/<%= baitulmaal._id %>" class=" btn btn-outline-dark">View</a></td>
              </tr>
            <% } %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</body>
<script>
  
</script>
</html>

